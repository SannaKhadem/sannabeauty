<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ansiktsbehandling – SannaBeauty Clinic</title>
  <link rel="stylesheet" href="/css/BeerSlider.min.css">
  <link rel="stylesheet" href="/css/styles.css" />
  <link href="https://fonts.googleapis.com/css2?family=Great+Vibes&family=Cinzel+Decorative:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    .beer-slider {
      max-width: 600px;
      margin: 40px auto;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 0 15px rgba(0,0,0,0.15);
    }
    .treatment-detail {
      text-align: center;
      padding: 2rem;
    }
  </style>
</head>
<body class="treatment-page">
  <%- include('../partials/header') %>
  
  <section class="treatment-detail">
    <h2>Ansiktsbehandling</h2>
    <p>En avslappnande ansiktsbehandling som rengör, återfuktar och förnyar din hud. Perfekt för dig som vill få ett fräscht lyster och ta hand om din hud på djupet.</p>
  </section>

  <section class="treatment-detail">
    <%
      const grouped = {};
      images.forEach(img => {
        let treatment = img.treatment;
        let phase = img.phase;
        let group = img.group;
        if (!treatment || !phase) {
          const parsed = img.parsed;
          if (parsed) { treatment = parsed.treatment; phase = parsed.phase; group = parsed.group; }
        }
        if (treatment === 'ansiktsbehandling' && phase && group != null) {
          if (!grouped[group]) grouped[group] = {};
          grouped[group][phase] = img;
        }
      });
      const sortedGroups = Object.entries(grouped).sort(([a],[b]) => a - b);
    %>
    <% if (sortedGroups.length === 0) { %>
      <p>Inga kompletta före-och-efter bilder hittades.</p>
    <% } else { %>
      <% sortedGroups.forEach(([groupId, pair]) => { if (pair.före && pair.efter) { %>
        <div class="beer-slider" data-beer-label="Efter">
          <img src="<%= pair.efter.path %>" alt="efter <%= groupId %>" />
          <div class="beer-reveal" data-beer-label="Före">
            <img src="<%= pair.före.path %>" alt="före <%= groupId %>" />
          </div>
        </div>
      <% } }) %>
    <% } %>
  </section>
  <div class="back-link-container">
    <a href="/behandlingar" class="back-link">
      <i class="fa fa-arrow-left"></i> Tillbaka till behandlingar
    </a>
  </div>

  <%- include('../partials/footer') %>

  <script src="/js/BeerSlider.min.js"></script>
  <script>
    window.addEventListener('DOMContentLoaded', function () {
      document.querySelectorAll('.beer-slider').forEach(slider => new BeerSlider(slider));
    });
  </script>
  <script src="/js/main.js?v=1"></script>
</body>
</html>